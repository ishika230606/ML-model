# Deep-Learning-Model
The dataset, with variations in lighting, angles, and backgrounds, is used to train and evaluate a binary CNN classifier. Images are resized to 224 × 224 pixels, normalized, and augmented with rotation and horizontal flipping to improve generalization.
